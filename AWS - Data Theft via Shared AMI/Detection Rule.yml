// Note: Always use the table name `DetectionTable` for your queries.
DetectionTable
| where eventSource contains "ec2.amazonaws.com"
| where eventName contains "ModifyImageAttribute"
| where requestParameters.attributeType contains "launchPermission"
